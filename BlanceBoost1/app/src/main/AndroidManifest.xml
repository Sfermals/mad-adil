<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Add Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BLanceBoost1"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Fitness Activity -->
        <activity android:name=".FitnessActivity" android:exported="true" />

        <!-- Nutrition Activity -->
        <activity android:name=".NutritionActivity" android:exported="true" />

        <!-- WebView Activity -->
        <activity android:name=".WebViewActivity" android:exported="true" />

        <!-- Breakfast Activity -->
        <activity android:name=".BreakfastActivity" android:exported="true"></activity>

        <!-- Profiles Activity -->
        <activity android:name=".ProfilesActivity" android:exported="true"></activity>

    </application>

</manifest>
